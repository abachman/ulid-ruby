require "spec_helper"

RSpec.describe Ulid::Ruby do
  it "has a version number" do
    expect(Ulid::Ruby::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
